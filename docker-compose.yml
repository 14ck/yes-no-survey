services:
  yes-no-survey:
    build: .
    container_name: yes-no-survey
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
